{
  "name": "focus-pomodoro",
  "version": "1.0.0",
  "description": "Focus Pomodoro — study timer with a distraction guard, as a native iOS/Android app",
  "private": true,
  "scripts": {
    "copy-web": "node -e \"require('fs').copyFileSync('../pomodoro.html','www/index.html')\"",
    "sync": "npm run copy-web && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/haptics": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
